<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.familybiz.greg.taqueue" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Example" >
        <activity
            android:name="com.familybiz.greg.taqueue.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.familybiz.greg.taqueue.MoreInformationActivity"
            android:label="@string/title_activity_more_information"
            android:parentActivityName="com.familybiz.greg.taqueue.MainActivity"
            android:theme="@style/Theme.Example" >
        </activity>
    </application>

</manifest>
